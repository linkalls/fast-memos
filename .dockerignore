# Git files
.git
.gitignore

# VSCode specific
.vscode/

# Database file (should be volume mounted)
memo_app.db
memo_app.db-journal # SQLite journal file

# Log files
*.log

# Temporary files
tmp/
temp/

# Markdown files (except README.md if needed, but generally not in app image)
# For this project, exclude all .md files from the app image context.
*.md

# Docker files themselves are not needed in the context after they are used.
Dockerfile
docker-compose.yml
.dockerignore

# Frontend specific
frontend/node_modules
frontend/dist # If frontend is built locally before Docker build (usually not the case with multi-stage)
frontend/.env* # Local environment variables for frontend (e.g. .env, .env.local)
frontend/.eslintcache # ESLint cache
# Add other frontend specific cache or build artifact directories if any
# e.g. frontend/.next, frontend/.nuxt, frontend/.svelte-kit, etc. if using those frameworks.
# For Vite, `dist` is the main one.
